# Локальные настройки сервера по умолчанию. Переменные окружения имеют приоритет.
ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [ speakingcharacter.ApplicationKt.module ]
  }
}
